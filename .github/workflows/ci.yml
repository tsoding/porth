name: CI
on: [push, pull_request]
jobs:
  builds:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v1
      - name: install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -qq nasm gcc cmake g++
      - name: install python dependencies
        run: |
          pip3 install mypy
      - name: run tests
        run: |
          mypy ./porth.py
          mypy ./test.py
          python3 ./test.py
          python3 ./test.py -f ./examples/
